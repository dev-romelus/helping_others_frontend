export const servicesType = Object.freeze({
    occasional_tasks: 'Occasional tasks',
    material_needs: 'Material needs'
})

export const serviceStatuses = Object.freeze({ 
    IN_PROGRESS: 'In progress', 
    SATISFIED:'Satisfied', 
    EXPIRED: 'Expired' 
});
